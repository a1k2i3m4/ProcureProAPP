# Server Configuration
NODE_ENV=development
PORT=5001

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=procurepro

# JWT Configuration
JWT_ACCESS_SECRET=change_me
JWT_SECRET=change_me
JWT_EXPIRE=7d
JWT_REFRESH_SECRET=change_me_too
JWT_REFRESH_EXPIRE=30d

# WhatsApp Business API Configuration
# IMPORTANT: keep WHATSAPP_API_TOKEN secret (do not commit real token)
WHATSAPP_API_TOKEN=
WHATSAPP_PHONE_ID=
# Base URL; override if needed (v22.0 recommended)
WHATSAPP_API_URL=https://graph.facebook.com/v22.0
WHATSAPP_VERIFY_TOKEN=procurepro_verify_token
# Default recipient for testing (E.164 digits only, without +)
WHATSAPP_TEST_NUMBER=77072303223
# Set true to disable real WhatsApp sends
WHATSAPP_MOCK_MODE=false

# WMS sync source (reporting-service)
REPORTING_SERVICE_URL=http://reporting-service:3100
# Optional sync tuning
WMS_REQUEST_TIMEOUT_MS=30000
WMS_SYNC_INTERVAL_MS=86400000

